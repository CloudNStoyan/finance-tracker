import { styled } from "../../../infrastructure/ThemeManager";

const DesktopDaysOfWeekStyled = styled.div`
  display: flex;
  > * {
    flex: 14%;
    font-size: 14px;
  }
`;

export default DesktopDaysOfWeekStyled;
